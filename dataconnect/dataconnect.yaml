specVersion: "v1beta"
serviceId: "arona-db"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "arona-db-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
